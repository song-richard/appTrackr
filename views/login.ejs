<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login to appTrackr</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
</head>
<h1 class="text-4xl font-bold mb-8 mt-8 text-blue-600 text-center">appTrackr</h1>
<h3 class="text-2xl mb-6 mt-8 text-blue-500 text-center">"Empowering Your Career Journey, One Application at a Time."</h3>
<body class="bg-gray-100 font-sans">

    <div class="container mx-auto p-8">
        <label class="text-2xl font-bold mb-4 text-blue-600">Register</label>
        <form id="registerForm" class="mb-8 grid grid-cols-1 gap-4">
            <input type="text" id="registerEmail" placeholder="Username" class="p-3 border rounded">
            <input type="password" id="registerPassword" placeholder="Password" class="p-3 border rounded">
            <button id="registerBtn" type="button" class="p-3 bg-blue-500 text-white rounded hover:bg-blue-600 transition duration-300">Register</button>
        </form>

        <label class="text-2xl font-bold mb-4 text-blue-600">Login</label>
        <form id="loginForm" class="mb-8 grid grid-cols-1 gap-4">
            <input type="text" id="loginEmail" placeholder="Username" class="p-3 border rounded">
            <input type="password" id="loginPassword" placeholder="Password" class="p-3 border rounded">
            <button id="loginBtn" type="button" class="p-3 bg-blue-500 text-white rounded hover:bg-blue-600 transition duration-300">Login</button>
        </form>

        <div id="successModal" class="modal hidden fixed top-0 left-0 w-full h-full bg-gray-800 bg-opacity-50 items-center justify-center">
            <div class="modal-content bg-white p-8 rounded-md text-center">
                <p class="text-xl font-bold text-green-500">Registered Successfully!</p>
                <button id="closeSuccessModalBtn" class="mt-4 p-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition duration-300">Close</button>
            </div>
        </div>

        <div id="errorModal" class="modal hidden fixed top-0 left-0 w-full h-full bg-gray-800 bg-opacity-50 items-center justify-center">
            <div class="modal-content bg-white p-8 rounded-md text-center">
                <p class="text-xl font-bold text-red-500">Incorrect Password</p>
                <p class="text-xl font-bold text-red-500">Please try again </p>
                <button id="closeModalBtn" class="mt-4 p-2 bg-blue-500 text-white rounded hover:bg-blue-600 transition duration-300">Close</button>
            </div>
        </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/bcryptjs/2.2.0/bcrypt.min.js" integrity="sha512-BJZhA/ftU3DVJvbBMWZwp7hXc49RJHq0xH81tTgLlG16/OkDq7VbNX6nUnx+QY4bBZkXtJoG0b0qihuia64X0w==" crossorigin="anonymous" referrerpolicy="no-referrer"></script>
    <script src="/hashPassword.js"></script>
    <script src="/loginFunc.js"></script>
</div>


</body>
</html>